% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{register_qr_solver}
\alias{register_qr_solver}
\alias{unregister_qr_solver}
\title{Register a custom quantile-regression solver}
\usage{
register_qr_solver(
  name,
  fit,
  exact,
  process_aware = FALSE,
  description = NULL,
  overwrite = FALSE,
  dependencies = NULL,
  version = NULL
)

unregister_qr_solver(name, quiet = FALSE)
}
\arguments{
\item{name}{Unique solver name.}

\item{fit}{Solver function following the contract above.}

\item{exact}{Whether the function exactly minimizes the linear QR objective.}

\item{process_aware}{Whether it estimates multiple quantiles jointly.}

\item{description}{Human-readable implementation description.}

\item{overwrite}{Replace an existing custom solver with the same name.}

\item{dependencies}{Optional named list of non-base objects used by `fit`.
Direct lexical dependencies are captured automatically; this argument is
useful for dependencies reached through dynamic lookup.}

\item{version}{Optional user-defined implementation version included in
checkpoint identities.}

\item{quiet}{Do not error when the requested custom extension is absent.}
}
\value{
Invisibly returns the normalized solver name.
}
\description{
The supplied function is called as `fit(X, y, weights, taus, control)` and
must return either a coefficient matrix or a list containing
`coefficients`. Coefficients must have one row per design column and one
column per quantile and must be in the original design units. Optional list
members are `flag` (zero denotes convergence), `iterations`, and `warnings`.
The function is responsible for applying case weights and minimizing the
claimed QR objective.
}
