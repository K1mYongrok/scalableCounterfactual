% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{cf_control}
\alias{cf_control}
\title{Configure counterfactual estimation}
\usage{
cf_control(
  nreg = 100L,
  trimming = 0.005,
  reported_quantiles = seq(0.1, 0.9, by = 0.1),
  alpha = 0.05,
  weighted_bootstrap = NULL,
  bootstrap_scheme = c("counterfactual", "empirical", "multiplier"),
  legacy_qr_shift = TRUE,
  legacy_weighted_quantile = FALSE,
  qr_precondition = TRUE,
  onestep_first_solver = c("auto", "br", "fn", "pfn"),
  onestep_bandwidth = c("hall_sheather", "bofinger"),
  qr_bootstrap_engine = c("standard", "xy_preprocess", "onestep", "auto"),
  robust_se = FALSE,
  bootstrap_max_retries = 2L,
  bootstrap_progress = interactive(),
  marginal_method = c("auto", "matrix", "chunked"),
  marginal_chunk_rows = 50000L,
  marginal_matrix_max_mb = 512,
  marginal_histogram_bins = 262144L,
  marginal_candidate_max = 2000000L,
  crossing_diagnostics = TRUE,
  crossing_tolerance = 1e-08,
  quantile_noncrossing = c("none", "rearrange"),
  cqr_right = FALSE,
  cqr_nsteps = 3L,
  cqr_first_cut = 0.1,
  cqr_later_cut = 0.05,
  cox_boundary = c("na", "error", "cap"),
  linear_backend = "auto",
  dr_backend = "auto",
  dr_workers = 1L,
  dr_warm_start = TRUE,
  dr_maxit = 100L,
  dr_tolerance = 1e-08,
  dr_precondition = TRUE,
  gpu_backend = c("cpu", "cuda"),
  gpu_precision = c("float64", "float32"),
  gpu_python = NULL,
  gpu_python_path = NULL,
  gpu_module_path = NULL,
  gpu_block_columns = 16L,
  gpu_qr_rho = 1,
  gpu_qr_maxit = 5000L,
  gpu_qr_tolerance = 1e-06,
  gpu_qr_allow_nonconvergence = FALSE
)
}
\arguments{
\item{nreg}{Number of conditional quantile regressions or distribution
regression thresholds.}

\item{trimming}{QR tail trimming fraction.}

\item{reported_quantiles}{Quantiles of the marginal distributions to report.}

\item{alpha}{Inference significance level.}

\item{weighted_bootstrap}{Deprecated logical compatibility argument. `TRUE`
maps to `bootstrap_scheme = "counterfactual"`; `FALSE` maps to
`"empirical"`.}

\item{bootstrap_scheme}{Bootstrap sampling law. `counterfactual` uses the
exponential-probability resampling in `Counterfactual` 1.2; `empirical`
uses ordinary within-group resampling; `multiplier` applies independent
exponential multipliers directly to observation weights.}

\item{legacy_qr_shift}{Reproduce the scalar trimming shift used in
`Counterfactual` 1.2 QR predictions.}

\item{legacy_weighted_quantile}{Reproduce the scale-sensitive
`Hmisc::wtd.quantile()` call in `Counterfactual` 1.2. This audit option
forces matrix marginalization and is not recommended for new analysis.}

\item{qr_precondition}{Apply an invertible QR design preconditioner internally
for numerical stability, then return coefficients in their original units.}

\item{onestep_first_solver}{Exact R solver used for the first
(nearest-median) quantile in the one-step process. `auto` follows
the large-sample choice in `qrprocess`: `br` below 100,000 observations
and `pfn` otherwise.}

\item{onestep_bandwidth}{Bandwidth rule used by the one-step process.
Hall-Sheather is the `qrprocess` default.}

\item{qr_bootstrap_engine}{QR bootstrap fitting engine. `standard` refits the
selected solver on every draw; `xy_preprocess` uses
[quantreg::boot.rq.pxy()] with the point estimates; `onestep` reuses
the point-estimate inverse Jacobians like `qrprocess`; `auto` selects the
matching specialized engine for supported solvers.}

\item{robust_se}{Use the bootstrap interquartile-range standard error.}

\item{bootstrap_max_retries}{Maximum deterministic replacement draws after
a failed bootstrap replication. A value of two allows three attempts per
requested replication.}

\item{bootstrap_progress}{Print completed replications and an estimated
remaining time while the bootstrap runs.}

\item{marginal_method}{Marginal-quantile implementation. `matrix` materializes
every conditional draw, `chunked` uses a bounded-memory weighted histogram
selection, and `auto` selects from the estimated draw-matrix size.}

\item{marginal_chunk_rows}{Maximum design rows predicted in one chunk.}

\item{marginal_matrix_max_mb}{Matrix-memory threshold used by `auto`.}

\item{marginal_histogram_bins}{Initial number of bins used by chunked
weighted selection.}

\item{marginal_candidate_max}{Maximum candidate draws retained in the final
exact-within-bin selection step; the histogram is refined when needed.}

\item{crossing_diagnostics}{Evaluate QR quantile crossing on the reference,
counterfactual-covariate, and comparison designs for the point estimate.}

\item{crossing_tolerance}{Minimum negative adjacent-quantile difference
counted as a crossing.}

\item{quantile_noncrossing}{Conditional-quantile monotonicity correction.
`none` retains the fitted process. `rearrange` sorts fitted conditional
quantiles within each covariate row. Rearrangement removes crossing while
preserving the conditional draw distribution and hence the reported
counterfactual marginal quantiles.}

\item{cqr_right}{Whether `cqr` uses right rather than left censoring.}

\item{cqr_nsteps}{Number of censored-QR selection/refitting steps; at least
three.}

\item{cqr_first_cut}{Weighted fraction trimmed after the first-stage
uncensoring-probability model.}

\item{cqr_later_cut}{Weighted positive-margin fraction trimmed during the
third and later CQR steps.}

\item{cox_boundary}{Policy for requested Cox/Kaplan-Meier quantiles above
the largest identified CDF value. `na` returns unidentified quantiles as
`NA`, `error` stops, and `cap` reproduces the legacy boundary behavior of
returning the final event time.}

\item{linear_backend}{Weighted least-squares backend for `loc`, `locsca`,
and `lpm`: robust base-R QR, Cholesky, RcppEigen-backed `fastglm`, or
`auto` (currently QR for backward-compatible numerical robustness).}

\item{dr_backend}{Binary-response backend for logit, probit, and cloglog distribution
regression: base-R `glm`, `fastglm`, `speedglm`, or `auto` (currently
`fastglm` when installed and base-R GLM otherwise).}

\item{dr_workers}{Workers used across distribution-regression thresholds.
This execution layer cannot be combined with group-level or
replication-level parallelism.}

\item{dr_warm_start}{Reuse neighboring-threshold coefficients as starting
values during sequential logit/probit/cloglog distribution regression.}

\item{dr_maxit}{Maximum IRLS iterations at each distribution threshold.}

\item{dr_tolerance}{IRLS convergence tolerance.}

\item{dr_precondition}{Apply an invertible weighted-design transformation
before logit/probit/cloglog fitting and return coefficients in original units.}

\item{gpu_backend}{Prediction and marginalization device. `cuda` uses the
optional CuPy runtime; `cpu` preserves the dependency-free default.}

\item{gpu_precision}{CUDA arithmetic precision. `float64` is the auditable
default. `float32` is experimental.}

\item{gpu_python}{Optional Python executable containing CuPy.}

\item{gpu_python_path}{Optional project-local Python package directory.}

\item{gpu_module_path}{Optional development override for the bundled module.}

\item{gpu_block_columns}{Quantile or threshold columns per CUDA block.}

\item{gpu_qr_rho}{ADMM penalty for the experimental `cuda_admm` QR solver.}

\item{gpu_qr_maxit}{Maximum ADMM iterations.}

\item{gpu_qr_tolerance}{ADMM primal and dual convergence tolerance.}

\item{gpu_qr_allow_nonconvergence}{Allow an experimental `cuda_admm` result
that reached its iteration limit to be returned with diagnostics. The
default stops before decomposition or inference.}
}
\value{
A validated control list.
}
\description{
Configure counterfactual estimation
}
