% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_isolated.R
\name{benchmark_qr_scaling}
\alias{benchmark_qr_scaling}
\title{Isolated-process QR scaling benchmark}
\usage{
benchmark_qr_scaling(
  formula,
  data,
  group,
  weights = NULL,
  solvers = c("fn", "qfnb", "pfnb", "proqreg", "profn"),
  reference_solver = "pfnb",
  control = cf_control(crossing_diagnostics = FALSE),
  sample_sizes = c(5000L, 20000L, 100000L),
  seed = 20260719L,
  point_workers = 1L,
  repetitions = 3L,
  warmup = 1L,
  randomize_order = TRUE,
  rss_poll_interval_ms = 25L,
  timeout_seconds = Inf,
  checkpoint_path = NULL,
  resume = TRUE
)
}
\arguments{
\item{formula}{Outcome and covariate formula.}

\item{data}{Analysis data.}

\item{group}{Binary group column or vector.}

\item{weights}{Optional positive sampling weights.}

\item{solvers}{Solvers to benchmark.}

\item{reference_solver}{Solver used for numerical differences.}

\item{control}{A [cf_control()] object.}

\item{sample_sizes}{One or more stratified sample sizes.}

\item{seed}{Sampling seed.}

\item{point_workers}{Identical worker count for every solver.}

\item{repetitions}{Recorded repetitions per solver and sample size.}

\item{warmup}{Discarded warmup repetitions per solver and sample size.}

\item{randomize_order}{Randomize solver order within each size/repetition.}

\item{rss_poll_interval_ms}{RSS sampling interval in milliseconds.}

\item{timeout_seconds}{Per-process timeout; `Inf` disables it.}

\item{checkpoint_path}{Optional RDS checkpoint updated after every recorded
solver run. An interrupted benchmark resumes completed runs by default.}

\item{resume}{Reuse a compatible `checkpoint_path` when it exists.}
}
\value{
A list containing repeat-level `raw` results and a `summary`.
}
\description{
Runs each solver and repetition in a fresh R process. In addition to elapsed
time and numerical differences, it samples the operating system resident set
size (RSS) of the worker process. This avoids contamination from memory left
behind by solvers run earlier in the same R session.
}
