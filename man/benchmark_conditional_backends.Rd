% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_backends.R
\name{benchmark_conditional_backends}
\alias{benchmark_conditional_backends}
\title{Benchmark non-QR conditional-model backends}
\usage{
benchmark_conditional_backends(
  formula,
  data,
  group,
  weights = NULL,
  model = c("loc", "locsca", "logit", "probit", "cloglog", "lpm"),
  backends = NULL,
  reference_backend = NULL,
  control = cf_control(),
  sample_n = NULL,
  seed = 20260719L,
  point_workers = 1L
)
}
\arguments{
\item{formula}{Outcome and covariate formula.}

\item{data}{Analysis data.}

\item{group}{Binary group column or vector.}

\item{weights}{Optional positive sampling weights.}

\item{model}{One of `loc`, `locsca`, `logit`, `probit`, `cloglog`, or `lpm`.}

\item{backends}{Linear backends for `loc`, `locsca`, and `lpm`, or binary
distribution-regression backends for `logit`, `probit`, and `cloglog`.}

\item{reference_backend}{Backend used for numerical differences.}

\item{control}{A [cf_control()] object.}

\item{sample_n}{Optional stratified benchmark sample size.}

\item{seed}{Sampling seed.}

\item{point_workers}{Identical worker count for every solver.}
}
\value{
A data frame of timing, memory, diagnostics, and numerical parity.
}
\description{
Every backend receives the same prepared outcomes, design matrices, group
split, weights, thresholds, and execution settings. Use
[benchmark_qr_solvers()] for quantile-regression solvers.
}
