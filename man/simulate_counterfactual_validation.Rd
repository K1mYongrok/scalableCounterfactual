% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_validation.R
\name{simulate_counterfactual_validation}
\alias{simulate_counterfactual_validation}
\title{Monte Carlo validation of counterfactual QR decomposition}
\usage{
simulate_counterfactual_validation(
  replications = 100L,
  n_per_group = 1000L,
  scenarios = names(simulation_scenarios()),
  solver = "fn",
  nreg = 49L,
  trimming = 0.02,
  reported_quantiles = c(0.1, 0.5, 0.9),
  weighted = FALSE,
  bootstrap_reps = 0L,
  bootstrap_scheme = "counterfactual",
  workers = 1L,
  seed = 20260809L,
  progress = interactive(),
  checkpoint_dir = NULL,
  max_retries = 1L,
  task_timeout_seconds = Inf
)
}
\arguments{
\item{replications}{Monte Carlo replications per scenario.}

\item{n_per_group}{Observations generated in each group.}

\item{scenarios}{Any subset of `location_shift`, `composition_shift`, and
`combined_scale_shift`.}

\item{solver}{Registered QR solver.}

\item{nreg}{Conditional-quantile grid size.}

\item{trimming}{Conditional QR trimming.}

\item{reported_quantiles}{Marginal quantiles evaluated.}

\item{weighted}{Generate independent positive sampling weights.}

\item{bootstrap_reps}{Bootstrap replications within each Monte Carlo draw.}

\item{bootstrap_scheme}{Bootstrap sampling scheme.}

\item{workers}{Parallel Monte Carlo workers. Nested bootstrap workers remain
fixed at one.}

\item{seed}{Master simulation seed.}

\item{progress}{Print completed simulation tasks.}

\item{checkpoint_dir}{Optional directory for replication-level checkpoints.
Completed tasks are reused when the full simulation signature matches.}

\item{max_retries}{Deterministic replacement attempts after a failed task.}

\item{task_timeout_seconds}{Best-effort elapsed-time limit for one task, or
`Inf`. Compiled solvers that do not check interrupts may exceed this limit.}
}
\value{
An object of class `cf_simulation_validation` containing raw errors,
  aggregate performance, failures, and run settings.
}
\description{
Simulates Gaussian linear designs with analytically known structure,
composition, and total quantile effects. The built-in scenarios separately
exercise pure structure, pure composition, and combined location/scale
changes. Optional nested bootstrap runs also report pointwise and uniform
empirical coverage.
}
