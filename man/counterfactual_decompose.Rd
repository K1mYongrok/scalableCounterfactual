% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomposition_engine.R
\name{counterfactual_decompose}
\alias{counterfactual_decompose}
\title{Estimate a counterfactual quantile decomposition}
\usage{
counterfactual_decompose(
  formula,
  data,
  group,
  weights = NULL,
  model = "qr",
  solver = NULL,
  control = cf_control(),
  bootstrap_reps = 0L,
  point_workers = 1L,
  bootstrap_workers = 1L,
  checkpoint_dir = NULL,
  seed = 20260719L,
  censoring = NULL,
  event = NULL
)
}
\arguments{
\item{formula}{Outcome and covariate formula. The formula must include an
intercept. Intercept-only formulas are supported for all models except
Cox regression, which also requires at least one covariate.}

\item{data}{Data frame containing analysis variables.}

\item{group}{Binary group column name or vector. Zero is the reference group
and one is the comparison group.}

\item{weights}{Optional positive sampling-weight column name or vector.}

\item{model}{Conditional-distribution model: `qr`, `cqr`, `loc`, `locsca`,
`cox`, `logit`, `probit`, `cloglog`, or `lpm`.}

\item{solver}{QR solver: `br`, `fn`, `pfn`, `qfnb`, `pfnb`, `proqreg`,
`profn`, `onestep`, experimental `cuda_admm`, or `auto`. CQR accepts the
exact solvers `br`, `fn`, `pfn`, `qfnb`, `pfnb`, and `auto`. Leave `NULL`
for other models.}

\item{control}{A [cf_control()] object.}

\item{bootstrap_reps}{Number of bootstrap replications.}

\item{point_workers}{Workers used for the two group-specific point fits. If
`bootstrap_workers = 1`, the standard bootstrap also reuses up to two of
these workers for the two group fits within each replication.}

\item{bootstrap_workers}{Independent bootstrap-replication workers.}

\item{checkpoint_dir}{Optional persistent checkpoint directory.}

\item{seed}{Random seed.}

\item{censoring}{Censoring-point column, vector, or scalar for `model =
"cqr"`.}

\item{event}{Optional 0/1 event-status column or vector for `model = "cox"`.
If omitted, every outcome is treated as an observed event.}
}
\value{
An object of class `cfdecomp`.
}
\description{
Estimate a counterfactual quantile decomposition
}
\examples{
set.seed(11)
example_data <- data.frame(
  outcome = rnorm(120),
  x = rnorm(120),
  group = rep(0:1, each = 60),
  weight = runif(120, 0.5, 1.5)
)
example_fit <- counterfactual_decompose(
  outcome ~ x,
  data = example_data,
  group = "group",
  weights = "weight",
  model = "qr",
  solver = "fn",
  control = cf_control(
    nreg = 5,
    reported_quantiles = c(0.25, 0.5, 0.75),
    crossing_diagnostics = FALSE
  ),
  seed = 11
)
as.data.frame(example_fit)
}
